ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "false"
  path: /
  hosts:
    ## 로컬PC hosts 파일에 아래 도메인을 등록해서 접속해야 Nginx에서 포워딩 됩니다.
    - kibana.andy.com
